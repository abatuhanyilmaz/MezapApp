import 'package:flutter/material.dart';

class Takipcilerim extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("Takipcilerim")));
  }
}
