import 'package:flutter/material.dart';

class Raporlarim extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("Raporlarim")));
  }
}
